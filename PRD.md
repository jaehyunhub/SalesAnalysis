# PRD: CU 편의점 매출 분석 플랫폼 (ConveniSight)

## Context

CU 편의점 점주가 어떤 상품이 잘 팔리는지 데이터 기반으로 파악할 수 없는 문제를 해결하기 위한 웹 플랫폼. 날씨, 휴일, 근처 학교 이벤트 등 다양한 환경 변수와 매출 데이터를 결합 분석하여 **폐기율 감소**, **매출 증대**, **행사 아이템 이익율 극대화**를 목표로 한다.

---

## 1. 문제 정의

| 문제 | 현재 상태 | 영향 |
|------|----------|------|
| 매출 분석 불가 | 어떤 상품이 잘 팔리는지 감으로 판단 | 재고 과잉/부족 발생 |
| 환경 변수 미반영 | 날씨, 이벤트에 따른 수요 변화 대응 불가 | 폐기율 증가, 기회 손실 |
| 본사 행사 아이템 판단 어려움 | 행사 아이템 이익율/적합성 파악 불가 | 수익성 낮은 행사 참여 |

## 2. 타겟 사용자

- **Primary**: CU 편의점 점주 (1인 운영 ~ 소규모)
- **Pain Point**: 데이터 분석 도구 없이 경험에만 의존한 재고/발주 관리
- **기술 수준**: 스마트폰/PC 기본 사용 가능, 전문 분석 도구 미경험

## 3. 제안 해결책

### 3.1 핵심 기능

#### F1. 매출 데이터 입력
- **POS 스크린샷 OCR 인식**: 점주가 POS 화면을 촬영/캡처하여 업로드하면 AI OCR로 매출 데이터 자동 추출
  - Tesseract OCR + 후처리 파이프라인 (CU POS 화면 특화)
  - 인식 결과 점주 확인/수정 UI 제공
- **엑셀/CSV 업로드**: CU POS에서 내보낸 매출 데이터 파일 업로드
  - 컬럼 자동 매핑 + 수동 매핑 지원
  - 중복 데이터 감지 및 처리

#### F2. 환경 변수 수집
- **자동 수집 (공공 API)**:
  - 기상청 단기예보 API → 날씨/기온/강수
  - 공휴일 API (data.go.kr) → 공휴일/대체휴일
- **수동 입력**:
  - 근처 학교 이벤트 (체육대회, 축제, 시험기간, 방학)
  - 지역 행사 (축제, 마라톤, 공사 등)
  - 점포 주변 특이사항 (도로 공사, 신규 아파트 입주 등)

#### F3. 매출 분석 대시보드
- **시간 단위 분석**: 일별 / 주별 / 월별 / 년별
- **시간대별 분석**: 시간대별 매출 패턴 (아침/점심/저녁/심야)
- **카테고리별 분석**: 음료, 식품, 생활용품, 담배 등
- **상품별 분석**: 개별 상품 매출 추이
- **환경 변수 상관관계**: 날씨-매출, 이벤트-매출 상관 분석
  - "비 오는 날 우산 매출 +300%" 등 인사이트 자동 생성

#### F4. 예측 및 추천
- **수요 예측**: 과거 데이터 + 환경 변수 기반 상품별 수요 예측
- **발주 추천**: 예측 기반 적정 발주량 제안
- **폐기 위험 알림**: 폐기 가능성 높은 상품 사전 알림

#### F5. 본사 행사 아이템 분석
- **행사 아이템 이익율 계산기**: 입고가, 판매가, 예상 판매량 기반 이익율 산출
- **내 점포 적합성 분석**: 과거 유사 상품 매출 데이터 기반 해당 점포에서의 예상 판매량 추정
- **행사 참여 의사결정 지원**: "참여 시 예상 이익 vs 미참여" 비교 뷰

### 3.2 기능 우선순위 (MoSCoW)

| 우선순위 | 기능 |
|---------|------|
| **Must Have** | 엑셀/CSV 업로드, 기본 매출 대시보드 (일/월별, 카테고리별), 상품별 매출 조회 |
| **Should Have** | POS 스크린샷 OCR, 날씨/휴일 API 연동, 시간대별 분석, 환경변수 상관관계 |
| **Could Have** | 수요 예측, 발주 추천, 폐기 위험 알림, 본사 행사 이익율 분석 |
| **Won't Have (v1)** | POS 실시간 연동, 다중 점포 관리, 모바일 앱 |

## 4. 기술 아키텍처

### 4.1 기술 스택

| 레이어 | 기술 | 선택 이유 |
|--------|------|----------|
| **프론트엔드** | Next.js 15 + TypeScript + Tailwind CSS | SSR/SSG 지원, 대시보드 SEO 불필요하나 빠른 초기 로딩 |
| **백엔드 API** | FastAPI (Python) | 데이터 분석 라이브러리(Pandas, NumPy) 네이티브 활용 |
| **OCR** | Tesseract + Python Pillow | 오픈소스, CU POS 화면 커스텀 학습 가능 |
| **차트** | Recharts 또는 Chart.js | React 네이티브 차트 라이브러리 |
| **DB** | PostgreSQL | 시계열 매출 데이터 + JSON 지원 |
| **캐시** | Redis | 대시보드 쿼리 결과 캐싱 |
| **인프라** | Docker Compose | 로컬 개발 및 배포 일관성 |

### 4.2 시스템 구조

```
[Next.js Frontend :3000]
        ↓
[FastAPI Backend :8000]
    ├── /api/sales       → 매출 데이터 CRUD
    ├── /api/upload       → 엑셀/CSV/스크린샷 업로드
    ├── /api/analysis     → 분석 결과 조회
    ├── /api/weather      → 날씨 데이터 (기상청 API 프록시)
    ├── /api/events       → 이벤트/환경변수 관리
    └── /api/promotion    → 본사 행사 아이템 분석
        ↓
[PostgreSQL :5432]  [Redis :6379]
```

### 4.3 주요 DB 테이블

```
sales_records     → 매출 원본 데이터 (날짜, 시간, 상품, 수량, 금액)
products          → 상품 마스터 (바코드, 이름, 카테고리, 입고가)
weather_data      → 일별 날씨 데이터
events            → 이벤트/환경변수 (학교 축제, 공휴일 등)
promotions        → 본사 행사 아이템 정보
upload_history    → 업로드 이력 관리
```

## 5. 목표 및 성공 지표

| 지표 | 목표 | 측정 방법 |
|------|------|----------|
| 폐기율 | **10% 미만** | (폐기 금액 / 총 입고 금액) × 100 |
| 매출 증가 | **30% 이상** | 도입 전후 동기간 매출 비교 |
| 행사 이익율 | **극대화** | 행사 참여 건별 실제 이익율 추적 |
| 사용자 만족도 | 주 3회 이상 접속 | 접속 빈도 로그 |

## 6. 개발 로드맵 (Phase별)

### Phase 1: MVP (4주)
- 프로젝트 셋업 (Next.js + FastAPI + PostgreSQL + Docker)
- 사용자 인증 (로그인/회원가입)
- 엑셀/CSV 업로드 및 파싱
- 기본 매출 대시보드 (일별/월별, 카테고리별 차트)
- 상품별 매출 조회

### Phase 2: 환경 변수 연동 (3주)
- 기상청 API 연동 (날씨/기온)
- 공휴일 API 연동
- 이벤트 수동 입력 UI
- 시간대별 분석 차트
- 환경 변수-매출 상관관계 시각화

### Phase 3: OCR + 고급 분석 (3주)
- POS 스크린샷 OCR 파이프라인 구축
- OCR 결과 확인/수정 UI
- 수요 예측 모델 (기본 통계 기반)
- 폐기 위험 알림

### Phase 4: 행사 분석 + 최적화 (2주)
- 본사 행사 아이템 이익율 계산기
- 점포 적합성 분석
- 대시보드 성능 최적화 (Redis 캐싱)
- 발주 추천 기능

## 7. 프로젝트 구조 (예상)

```
convienceStore/
├── docker-compose.yml
├── PRD.md
├── frontend/
│   ├── src/
│   │   ├── app/              # Next.js App Router
│   │   │   ├── dashboard/    # 대시보드 페이지
│   │   │   ├── upload/       # 데이터 업로드 페이지
│   │   │   ├── analysis/     # 상세 분석 페이지
│   │   │   ├── promotion/    # 행사 분석 페이지
│   │   │   └── settings/     # 설정 (이벤트 관리 등)
│   │   ├── components/       # 공용 컴포넌트
│   │   ├── lib/              # 유틸리티
│   │   └── types/            # TypeScript 타입
│   ├── package.json
│   └── Dockerfile
├── backend/
│   ├── app/
│   │   ├── main.py           # FastAPI 엔트리포인트
│   │   ├── routers/          # API 라우터
│   │   ├── models/           # SQLAlchemy 모델
│   │   ├── schemas/          # Pydantic 스키마
│   │   ├── services/         # 비즈니스 로직
│   │   │   ├── ocr.py        # OCR 처리
│   │   │   ├── analysis.py   # 분석 로직
│   │   │   ├── weather.py    # 날씨 API
│   │   │   └── prediction.py # 수요 예측
│   │   └── core/             # 설정, DB, 인증
│   ├── requirements.txt
│   └── Dockerfile
└── README.md
```

## 8. 리스크 및 대응

| 리스크 | 영향 | 대응 방안 |
|--------|------|----------|
| OCR 인식률 저조 | 데이터 품질 저하 | CU POS 화면 특화 전처리, 수동 수정 UI 제공 |
| 공공 API 장애 | 환경 변수 수집 실패 | 캐싱 + 수동 입력 폴백 |
| 점주 데이터 입력 부담 | 서비스 이탈 | 엑셀 템플릿 제공, OCR 자동화로 부담 최소화 |
| 분석 정확도 | 신뢰도 저하 | 통계 기반 시작 → 점진적 ML 모델 고도화 |

## 9. 검증 방법

1. **Phase 1 완료 후**: 샘플 CSV 데이터로 업로드 → 대시보드 차트 정상 표시 확인
2. **Phase 2 완료 후**: 기상청 API 호출 → 날씨-매출 상관관계 차트 표시 확인
3. **Phase 3 완료 후**: CU POS 스크린샷 업로드 → OCR 인식 → 데이터 저장 플로우 확인
4. **전체 E2E**: Docker Compose로 전체 서비스 기동 → 데이터 입력부터 분석 결과 조회까지 전 과정 테스트

---

## 10. 핵심 결정 사항 요약

- **프로젝트**: 새 프로젝트 (convienceStore)
- **브랜드**: CU 편의점 특화
- **데이터 입력**: POS 스크린샷 OCR + 엑셀/CSV 업로드
- **외부 데이터**: 날씨/휴일은 공공 API, 학교 이벤트 등은 수동 입력
- **기술 스택**: Next.js + FastAPI + PostgreSQL + Redis + Docker
- **MVP 목표**: 엑셀 업로드 + 기본 매출 대시보드
